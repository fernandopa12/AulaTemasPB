<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AulaTemas" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Config cores primárias -->
        <item name="colorPrimary">@color/primaria</item>
        <item name="colorPrimaryVariant">@color/primariaVariante</item>

        <!-- Config cores secundárias -->
        <item name="colorSecondary">@color/secundaria</item>
        <item name="colorSecondaryVariant">@color/secundariaVariante</item>

        <!-- Config cor de fundo -->
        <item name="android:windowBackground">@color/corDeFundo</item>
    </style>

    <style name="Theme.AulaTemas" parent="Base.Theme.AulaTemas" />
</resources>